------------------------------------------
Readme for the webservice and test mockup
------------------------------------------
It is assumed that go is installed

It is assumed the PATHs have been set

export PATH=$PATH:/usr/local/go/bin
export GOPATH=$HOME/go

The app requires go packages to be installed


go run martini2.go


The service will be available listening on port 3000